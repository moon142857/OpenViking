# 教学知识库系统 - OpenViking API 能力评估与测试计划

## 1. 需求分析

### 1.1 业务场景
- **知识库构建**：导入大量教案、政策文件、教材背景知识
- **资源检索**：老师使用时查找政策解读、教材背景、相关教案
- **教案生成**：基于检索结果自动生成新教案
- **教案评价**：多维度对教案进行打分评价

### 1.2 核心关注点
- 性能：导入速度、搜索延迟、并发能力
- 召回率：语义搜索能否准确找到相关资源
- 评价系统：如何实现对教案的多维度评价

## 2. OpenViking API 能力评估

### 2.1 完全满足的能力

| 能力 | API | 评估 |
|------|-----|------|
| 语义搜索 | `POST /api/v1/search/find` | 支持 dense/sparse/hybrid embedding，rerank，level过滤，metadata过滤 |
| 层级检索 | `target_uri` + 目录递归 | `HierarchicalRetriever` 支持目录级语义搜索 |
| 内容多层级加载 | `GET /content/abstract`, `/content/overview`, `/content/read` | L0/L1/L2 三级上下文，非常适合教案的摘要/详情查看 |
| 文件导入 | `POST /api/v1/resources` | 支持目录批量导入，多种格式（PDF/Word/Excel/Markdown等） |
| 批量打包导入/导出 | `POST /api/v1/pack/import`, `/pack/export` | `.ovpack` 格式支持全量备份恢复 |
| 会话管理 | `POST /api/v1/sessions/*` | 支持多轮对话上下文，可用于教案生成对话 |
| WebDAV 访问 | `/webdav/resources/*` | 可直接挂载为文件系统 |
| MCP 集成 | `/mcp` | 原生支持 Claude Code 等 MCP 客户端 |

### 2.2 部分满足的能力

| 能力 | 现状 | 缺口 |
|------|------|------|
| 批量导入 | 支持目录导入，不支持多文件一次性上传 | 需客户端循环调用 `temp_upload` + `add_resource` |
| 元数据标签 | 无显式标签系统，可用目录结构 + `filter` 替代 | 需设计目录命名规范模拟标签 |

### 2.3 不满足的能力（需外部系统补充）

| 能力 | 原因 | 建议方案 |
|------|------|---------|
| **教案自动生成** | OpenViking 是上下文数据库，不提供 LLM 生成 | 对接 Claude/GPT/Kimi 等外部 LLM，通过 `find` 检索上下文后送入 LLM 生成 |
| **教案多维度评价** | 无内置评价模型 | 使用外部 LLM 做评价（教学设计、教学目标、互动性、创新性等维度） |

### 2.4 总体评估结论

**OpenViking 非常适合作为教学知识库的"语义检索与管理层"**，在以下方面表现优秀：
- 教案/政策/背景知识的语义存储和检索
- 多级内容加载（快速浏览摘要 → 深入查看详情）
- 目录化组织（年级/学科/主题层级）
- 与 LLM 的集成（通过 MCP 或 API）

**需要补充的层**：
- **生成层**：外部 LLM（Claude/GPT-4/Kimi）负责教案生成和评价
- **应用层**：教学业务逻辑（用户管理、评价模板、生成 Prompt 工程）

## 3. 测试计划

### 3.1 测试数据设计

在 `viking://resources/teaching_kb/` 下构建测试数据集：

```
teaching_kb/
├── policies/              # 教育政策文件（3-5个）
├── textbooks/             # 教材背景知识
│   ├── 小学三年级/
│   │   ├── 数学/
│   │   └── 语文/
│   └── 初中八年级/
│       ├── 数学/
│       └── 物理/
└── lesson_plans/          # 教案
    ├── 小学三年级/
    │   ├── 数学/
    │   └── 语文/
    └── 初中八年级/
        ├── 数学/
        └── 物理/
```

数据规模：
- 政策文件：3 个
- 教材背景知识：8 个（2年级 × 2学科 × 2主题）
- 教案：12 个（每主题 1-2 个教案）
- 总计：~23 个文档

### 3.2 测试用例

#### TC-1: 批量导入测试
- **目标**：测试目录批量导入性能和正确性
- **步骤**：
  1. 创建本地测试数据目录
  2. 通过 `/resources` API 导入 `teaching_kb` 目录
  3. 检查导入后的目录结构（`fs/tree`）
  4. 检查向量索引状态（`debug/vector/count`）
  5. 检查 abstract/overview 是否自动生成
- **指标**：导入耗时、向量生成耗时、文件完整性

#### TC-2: 语义搜索召回率测试 - 政策查找
- **目标**：测试查找教育政策的能力
- **查询**："双减政策下如何减少学生作业负担"
- **期望**：召回 `双减政策解读.md`，分数 > 0.8
- **指标**：Top-1 分数、目标命中率

#### TC-3: 语义搜索召回率测试 - 教材背景查找
- **目标**：测试按年级/学科查找背景知识
- **查询**："小学三年级数学分数的概念"
- **target_uri**：`viking://resources/teaching_kb/textbooks/小学三年级/数学`
- **期望**：召回 `分数初步认识-背景知识.md`
- **指标**：Top-1 分数、目标命中率

#### TC-4: 语义搜索召回率测试 - 教案查找
- **目标**：测试查找相似教案的能力
- **查询**："设计一个关于分数初步认识的数学教案"
- **期望**：召回 `分数初步认识-教案1.md` 和 `分数初步认识-教案2.md`
- **指标**：Top-K 召回率、分数分布

#### TC-5: 跨类型搜索测试
- **目标**：测试不指定类型时，系统能否混合召回政策+背景+教案
- **查询**："分数教学"
- **target_uri**：`viking://resources/teaching_kb`
- **期望**：同时召回政策、背景知识、教案
- **指标**：多样性、各类型占比

#### TC-6: Level 过滤测试
- **目标**：测试 L0(abstract) vs L2(content) 搜索效果差异
- **查询**："分数教学"
- **参数**：分别测试 `level=0`, `level=2`, `level=null`
- **指标**：结果数量、相关性变化

#### TC-7: 搜索性能基准测试
- **目标**：测试搜索延迟和并发能力
- **步骤**：
  1. 单次搜索延迟（冷启动/热启动）
  2. 10次串行搜索平均延迟
  3. 5并发搜索延迟分布
- **指标**：P50/P95/P99 延迟

#### TC-8: 内容读写性能测试
- **目标**：测试教案内容加载性能
- **步骤**：
  1. 读取 L0 abstract（预期 < 10ms）
  2. 读取 L1 overview（预期 < 10ms）
  3. 读取 L2 full content（预期 < 20ms）
- **指标**：各级内容加载延迟

#### TC-9: 生成与评价架构可行性测试
- **目标**：验证 OpenViking + 外部 LLM 的集成模式是否可行
- **步骤**：
  1. 使用 `find` 检索相关资源
  2. 模拟将检索结果送入外部 LLM 生成教案
  3. 模拟使用外部 LLM 对教案进行多维度评价
- **指标**：检索上下文质量、Token 使用量

### 3.3 评价系统设计建议

由于 OpenViking 不直接提供评价能力，建议架构：

```
用户输入教案 → OpenViking(find)检索相关标准/优秀教案 → 
外部 LLM(评价Prompt) → 多维度评分 → 存储评价结果到 OpenViking
```

评价维度示例：
1. 教学目标明确性（0-10分）
2. 教学内容科学性（0-10分）
3. 教学方法创新性（0-10分）
4. 学生互动设计（0-10分）
5. 重难点把握（0-10分）
6. 整体质量（0-10分）

## 4. 预期输出

- 测试执行报告（MD 格式）
- 召回率/精度分析表
- 性能基准数据
- 架构建议与 Gap 分析
