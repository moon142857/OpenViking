# OpenViking 切分、入库与检索最佳策略

> 基于 OpenViking v5.0 代码调研，针对 Markdown 解析、层次化检索与图文混排场景的最佳实践。

## 1. Markdown 解析与切分

- 解析阶段**不调用 LLM**，直接按标题结构生成 VikingFS 目录结构。
- 默认 `max_section_size = 2048 tokens`；叶子 section 超过才按段落进一步切分。
- 小 section（< 512 tokens）会与相邻 section 合并。
- 有子标题的 section 会变成**子目录**；叶子 section 变成 `.md` 文件。
- 每个最终的 `.md` 文件会作为一个独立的 L2（DETAIL）向量记录。
- 向量化时还有一层独立限制：`embedding.max_input_tokens` 默认 **4096 tokens**（`openviking_cli/utils/config/embedding_config.py:40`）。如果用的 embedder 上下文更大（如 Doubao Embedding Vision 支持 128k），需要把该配置同步调大，否则 `max_section_size` 设得再大也会被 4096 截断。

代码参考：`openviking/parse/parsers/markdown.py`、`openviking_cli/utils/config/embedding_config.py`、`openviking_cli/utils/config/embedding_config.py`

## 2. 层次化检索

`HierarchicalRetriever` 按 L0/L1/L2 三层检索：

- **L0**：`.abstract.md`（目录摘要）
- **L1**：`.overview.md`（目录总结，包含子文件摘要）
- **L2**：叶子文件（章节片段、图片、代码等）

流程：先全局搜 L0/L1 定位相关目录，再递归进入目录搜 L2 子节点。子节点分数会叠加上父目录分数。

代码参考：`openviking/retrieve/hierarchical_retriever.py`

## 3. 图片的处理方式（关键限制）

- Markdown 里的 `![alt](path)` 会被复制成**独立图片文件**，URI 改写成 `viking://`。
- **Markdown 本身只 embed 纯文本**，不会把其中引用的图片加入该 Markdown 的向量。
- 图片文件单独走 VLM 摘要 + 多模态 embed（如果 embedder 支持，如 VolcEngine multimodal）。
- 代码里没有在 Markdown 和图片之间自动建立 `related_uri` 或联合向量。

**结论**：题干文字和选项小图是各自独立的向量记录，检索时无法保证一起命中。

代码参考：`openviking/utils/embedding_utils.py`、`openviking/parse/image_rewrite.py`

## 4. 图文混排（教科书 / 试题）的最佳实践

### 方案 A：VLM/OCR 转文字（检索效果最好）

入库前把选项图的内容描述成文字，写回 Markdown：

```markdown
## 例题 3.2

已知函数 f(x)=...，求最大值。

选项：
- A: 抛物线开口向上，顶点 (1,2)
- B: ...
```

小选项图**不入库**，只保留文字。这样题干和选项信息都在同一个文本向量里，检索最稳。

### 方案 B：整题截图成单张大图

对图形密集型题目，把“题干 + 4 个选项”截成一张大图入库：

- VLM 摘要包含整道题语义。
- 多模态 embedder 可直接对整张图向量。
- 文字搜、以图搜图都可能命中。

牺牲文字可编辑性，但保留原始版面。

### 方案 C：丢弃无信息小图，保留大图

- 不保留 A/B/C/D 小图标、装饰图、logo。
- 保留大幅信息图、几何图、函数图像、表格、公式截图。
- 对含少量关键文字的图做 OCR，文字写回 Markdown 后丢弃图。

## 5. 什么时候图片值得入库？

| 图片类型 | 建议 |
|---------|------|
| 大幅信息图、流程图、几何图、函数图像 | **值得**（VLM 摘要 + 多模态 embed） |
| 表格、复杂公式截图 | **值得** |
| 小选项图、A/B/C/D 图标 | **不值得**（信息量低，制造噪声） |
| 装饰性图片、logo、页眉页脚 | **不值得** |
| 含关键文字但文字少 | **OCR 后丢弃图** |

代码里已有 `VLMProcessor.filter_meaningful_images` 可判断图片是否有意义，但当前解析流程未调用，未来可接入自动过滤。

## 总结

默认 pipeline 把图和文字拆成独立向量，对于“文字描述 + 多个选项图”的试题类内容，小选项图几乎无法辅助检索，反而会增加噪声和存储成本。

- 数学/物理/教科书类资源入库前，先跑一轮 VLM/OCR 把选项图转成文字描述。
- 保持 Markdown 标题结构清晰，让 OpenViking 按章节建立目录，发挥层次化检索优势。
- 只把真正信息密集的大图作为独立资源保留；小图、装饰图直接丢弃。
- 如需保留整题版面，优先把整道题截成一张大图入库，而不是拆成文字 + 多个小图。

## 6. 检索后的多模态理解：文字描述 + 原图一起给 VLM

前面提到，小选项图最好不入库，而是把图的内容用文字描述写进 Markdown。但这**不代表图片没有用了**——恰恰相反，图片在“检索后的理解阶段”非常有价值。

### 推荐流程

```
文本检索命中 Markdown 题目
        ↓
读取 Markdown 原文（含图片的文字描述）
        ↓
从 Markdown 中提取 viking:// 图片引用，读取原图
        ↓
把 [Markdown 文字 + 原始图片] 一起丢给 VLM
        ↓
VLM 完整理解题目，并生成同类题、举一反三
```

### 为什么这样最好？

| 阶段 | 作用 | 原因 |
|------|------|------|
| 入库前转文字 | **解决检索问题** | 让题干和选项信息都能被文本向量检索到 |
| 理解时给原图 | **解决理解问题** | VLM 直接看图，对几何图形、函数图像的理解比文字描述更准确 |
| VLM 生成同类题 | **解决生成问题** | 看到完整题目后，可以按相同知识点/题型出题 |

### 示例代码结构

```python
# 1. 文本检索命中题目
result = await viking.find("求二次函数最大值 选项图像", limit=5)
md_uri = result.resources[0].uri

# 2. 读取 Markdown 原文
md_content = await viking.read(md_uri)

# 3. 提取并读取原图（viking:// URI）
image_uris = extract_image_uris(md_content)
images = [await viking.read_bytes(uri) for uri in image_uris]

# 4. 给 VLM，要求举一反三
prompt = f"""
请理解下面这道题，并生成 3 道考查相同知识点、难度相近、但数据和场景不同的练习题，
给出完整题干和选项。

{md_content}
"""

response = await vlm.get_vision_completion_async(prompt=prompt, images=images)
```

### 注意事项

1. **检索回来的是否是完整一道题**
   - 如果题目很短（< 2048 tokens），通常就是一个完整 L2 文件。
   - 如果题目被切分成多个 `.md` 文件，需要把同一目录下的相邻文件拼接，或读 `.overview.md` 获取全貌。

2. **图片引用是 `viking://` URI**
   - 入库后图片引用已被改写成 `viking://resources/...`（`openviking/parse/image_rewrite.py`）。
   - 需要用 VikingFS 的 `read_bytes` 接口读取，不能直接当本地文件路径。

3. **给 VLM 时文字和图都要给**
   - 只给文字：VLM 可能漏掉图像细节。
   - 只给图片：VLM 可能漏掉文字里的精确条件。
   - **文字 + 原图**：信息最完整。

4. **“举一反三”需要显式 prompt**
   - VLM 不会自动出题，要在 prompt 里明确要求“生成几道同类题”。

### 总结

> **检索靠文本化的 Markdown，理解靠原文 + 原图一起喂 VLM，生成靠 VLM 举一反三。**

这是目前 OpenViking 上图文混排内容最稳的用法：先把图里的信息转写成可检索的文字，保证能命中；命中后再把原图也交给 VLM，保证理解完整。

## 7. 为什么 OpenViking 不把文字和图片一起做 multimodal embedding？

把“题干文字 + 4 个选项图”作为一个整体输入 multimodal embedder，理论上确实会得到更完整、更精确的语义向量。但 OpenViking 当前**所有格式都没有采用这个方案**。

### 7.1 当前各格式的实际做法

| 格式 | 图片怎么处理 | 是否文+图一起做 embed |
|------|------------|---------------------|
| **Markdown** | 图片复制成独立文件，URI 改写成 `viking://` | ❌ 否，Markdown 只 embed 文字 |
| **PDF** | 转成 Markdown，图片内嵌为 `![...](path)` | ❌ 否，图独立 embed |
| **Word** | 提取图片到 media_dir，段落里插 `![...](path)` | ❌ 否，图独立 embed |
| **HTML** | 直接删除 `data:image` 内嵌图 | ❌ 否，图都不保留 |
| **独立图片文件** | 作为单独资源 | ✅ 是，摘要文字 + 图片本身一起做 multimodal embed |

代码证据：`openviking/utils/embedding_utils.py:522` 是唯一在入库时传 `images=[...]` 的地方，且只针对 `ResourceContentType.IMAGE`；`build_multimodal_embedding_input` 只在搜索查询侧使用，不在入库侧使用。

### 7.2 不做的原因

1. **语义单元难以自动划分**
   - 一道题可能跨多个段落、多个图片；一个段落也可能包含多道小题。
   - 自动判断“哪些文字和哪些图属于同一个语义单元”需要版面理解能力，当前解析器没有。

2. **模型限制**
   - 多模态 embedding 模型通常限制单请求图片数量、分辨率、总 token。
   - 一章有几十张图时，无法全部塞进一个 embedding。

3. **成本与延迟**
   - multimodal embedder 比 text embedder 贵得多、慢得多。
   - 如果每篇文档都做文+图联合 embed，入库成本会大幅上升。

4. **架构设计选择**
   - OpenViking v5.0 明确“解析阶段不调用 LLM”。
   - 文+图联合 embed 需要解析器/语义处理器做复杂的多模态调用，与当前“简单切片、独立向量化”的设计冲突。

5. **检索粒度问题**
   - 如果整章 + 所有图做一个联合向量，检索结果太粗。
   - 如果切成“每段文字 + 附近几张图”，切片逻辑又很难通用。

### 7.3 如果要改代码实现文+图联合 embed

思路：

1. 在 `MarkdownParser`/`PDFParser` 里识别“图文段落单元”（如一道题 = 题干文字 + N 张选项图）。
2. 把该单元生成一个 Context：
   ```python
   context.set_vectorize(Vectorize(text=题干文字, images=[选项A, 选项B, ...]))
   ```
3. 在 `embedding_utils.py` 里扩展 TEXT 分支，允许 Markdown 提取内嵌图片并加入 `Vectorize.images`。
4. 限制每单元图片数量，超过则 fallback 到只 embed 文字摘要。

这是一个涉及解析、向量化、存储的较大改动，目前代码没有实现。

### 7.4 当前结论

| 问题 | 答案 |
|------|------|
| 文+图一起 embed 是否更精确 | ✅ 是的，理论上更完整 |
| OpenViking 目前是否这样做 | ❌ 否，所有格式都分开处理或丢弃图片 |
| 为什么不做 | 语义单元难分、模型限制、成本高、与当前架构冲突 |
| 当前最佳实践 | 入库前把图转文字用于检索；检索后再把原文 + 原图一起给 VLM 理解 |
